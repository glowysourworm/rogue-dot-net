<UserControl x:Class="Rogue.NET.View.CreatingScenarioView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:common="clr-namespace:Rogue.NET.Common.View;assembly=Rogue.NET.Common" 
             xmlns:coreView="clr-namespace:Rogue.NET.Core.View;assembly=Rogue.NET.Core"
             Background="Transparent" 
             FontFamily="{StaticResource CentaurFont}"
             Height="220" Width="300" >
    <Border Background="Black" BorderBrush="Gray" BorderThickness="2" CornerRadius="10">
        <Grid Background="Transparent">
            <StackPanel Margin="8" HorizontalAlignment="Stretch">
                <TextBlock Text="Creating Scenario" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="16"  Foreground="White" FontSize="28" />
                <coreView:Smiley Height="45" Width="30" SmileyLineBlur="0" SmileyLineThickness="2"  SmileyColor="{Binding SmileyBodyColor}" SmileyLineColor="{Binding SmileyLineColor}"  SmileyMood="Happy" SmileyRadius="5" />
                <TextBlock Text="{Binding Message}" HorizontalAlignment="Center" Foreground="White" Margin="10"/>
                <common:ProgressBar Value="{Binding Progress}" Value2="{Binding Progress}"  ValueMax="100"  TextOn="False" HeaderTextOn="False"  BarColor1="GreenYellow" Margin="30,0,30,0" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
