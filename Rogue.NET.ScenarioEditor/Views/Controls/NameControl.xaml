<UserControl x:Class="Rogue.NET.ScenarioEditor.Views.Controls.NameControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:extension="clr-namespace:Rogue.NET.Common.Extension;assembly=Rogue.NET.Common"
             Width="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Foreground="White" Text="Name: " FontSize="16" VerticalAlignment="Center" Margin="8" />

        <TextBox x:Name="NameTB" Style="{StaticResource TextBoxStyle}" Margin="8" Grid.Column="1" />

        <Button extension:ButtonExtension.DialogResult="True" HorizontalAlignment="Right" Margin="8" Content="Ok" Grid.Column="1" Grid.Row="1" Width="80" Height="24" />
    </Grid>
</UserControl>
