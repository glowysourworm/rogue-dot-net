<UserControl x:Class="Rogue.NET.ScenarioEditor.Views.Assets.Doodad"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             FontSize="14"
             mc:Ignorable="d" Background="Black" d:DesignWidth="550">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Rogue.NET.Core;component/Resource/Converter.xaml" />
                <ResourceDictionary Source="pack://application:,,,/Rogue.NET.ScenarioEditor;component/Resources/Dictionaries/Converter.xaml" />
                <ResourceDictionary Source="pack://application:,,,/Rogue.NET.Common;component/Resource/Dictionaries/ButtonSpinner.xaml" />
                <ResourceDictionary>
                    <Style TargetType="TextBox">
                        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
                        <Setter Property="BorderBrush" Value="{DynamicResource NormalBorderBrush}" />
                        <Setter Property="Background" Value="{DynamicResource LightBrush}"/>
                    </Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="White" />
                    </Style>
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid>
            <StackPanel>
                <Grid Margin="10">
                    <Button Content="Edit Symbol" VerticalAlignment="Top" Padding="5" Width="100" HorizontalAlignment="Right" Click="CreateSymbol_Click" />
                    <Border HorizontalAlignment="Left" Height="48" Width="48" BorderBrush="White" BorderThickness="1" CornerRadius="3" >
                        <Border.Background>
                            <RadialGradientBrush>
                                <GradientStop Offset="0">
                                    <GradientStop.Color>
                                        <MultiBinding Converter="{StaticResource SymbolDetailsTemplateAuraToColorConverter}">
                                            <Binding Path="SymbolDetails.SmileyAuraColor" />
                                            <Binding Path="SymbolDetails.Type" />
                                        </MultiBinding>
                                    </GradientStop.Color>
                                </GradientStop>
                                <GradientStop Color="Transparent" Offset="1" />
                            </RadialGradientBrush>
                        </Border.Background>
                        <Image VerticalAlignment="Center" 
                               HorizontalAlignment="Center" 
                               Source="{Binding Path=SymbolDetails, Converter={StaticResource SymbolDetailsImageSourceConverter}}"
                               Stretch="None">
                        </Image>
                    </Border>
                </Grid>
                <Grid Margin="10">
                <TextBlock Text="Short Description" />
                    <TextBox Text="{Binding ShortDescription}" 
                         HorizontalAlignment="Right" 
                         AcceptsReturn="True"
                         HorizontalScrollBarVisibility="Disabled"
                         VerticalScrollBarVisibility="Auto"
                         TextWrapping="Wrap"
                         Height="60"
                         Width="300" />
                </Grid>
            <Grid Margin="10">
                <TextBlock Text="Long Description" />
                    <TextBox Text="{Binding LongDescription}" 
                         HorizontalAlignment="Right" 
                         AcceptsReturn="True"
                         HorizontalScrollBarVisibility="Disabled"
                         VerticalScrollBarVisibility="Auto"
                         TextWrapping="Wrap"
                         Height="100"
                         Width="300" />
                </Grid>
            <Grid Margin="10">
                <TextBlock Text="Is Unique" />
                <CheckBox IsChecked="{Binding IsUnique}" 
                         HorizontalAlignment="Right"  />
            </Grid>
            <Grid Margin="10">
                <CheckBox Grid.Column="1" IsChecked="{Binding IsAutomatic}" HorizontalAlignment="Right" VerticalAlignment="Center" />
                <TextBlock Foreground="White" Text="Automatic (Triggers when stepped on)" HorizontalAlignment="Left" VerticalAlignment="Center" />
            </Grid>
            <Grid Margin="10">
                <CheckBox Grid.Column="1" Grid.Row="1" IsChecked="{Binding IsVisible}" HorizontalAlignment="Right" VerticalAlignment="Center" />
                <TextBlock Grid.Row="1" Foreground="White" Text="Visible (before being stepped on)" HorizontalAlignment="Left" VerticalAlignment="Center" />
            </Grid>
            <Grid Margin="10">
                <CheckBox Grid.Column="1" Grid.Row="2" IsChecked="{Binding IsInvoked}" HorizontalAlignment="Right" VerticalAlignment="Center" />
                <TextBlock Grid.Row="2" Foreground="White" Text="Invoked (player must trigger manually)" HorizontalAlignment="Left" VerticalAlignment="Center" />
            </Grid>
            <Grid Margin="10">
                <CheckBox Grid.Column="1" Grid.Row="3" IsChecked="{Binding IsOneUse}" HorizontalAlignment="Right" VerticalAlignment="Center" />
                <TextBlock Grid.Row="3" Foreground="White" Text="One Use (as opposed to unlimited uses)" VerticalAlignment="Center" />
            </Grid>
            <Grid Margin="10">
                <ComboBox x:Name="AutomaticSpellCB" 
                      DisplayMemberPath="Name" 
                      SelectedItem="{Binding AutomaticMagicSpellTemplate, Mode=TwoWay}" 
                      SelectedValue="{Binding AutomaticMagicSpellTemplate.Name}"
                      SelectedValuePath="Name" Width="200" HorizontalAlignment="Right"/>
                <TextBlock Grid.Row="4" Foreground="White" Text="Automatic Spell" HorizontalAlignment="Left" VerticalAlignment="Center" />
            </Grid>
            <Grid Margin="10">
                <ComboBox x:Name="InvokedSpellCB" 
                          DisplayMemberPath="Name" 
                          SelectedItem="{Binding InvokedMagicSpellTemplate, Mode=TwoWay}" 
                          SelectedValue="{Binding InvokedMagicSpellTemplate.Name}"
                          SelectedValuePath="Name" Width="200" HorizontalAlignment="Right"/>
                <TextBlock Grid.Row="5" Foreground="White" Text="Manual (Invoked) Spell" HorizontalAlignment="Left" VerticalAlignment="Center" />
            </Grid>          
        </StackPanel>
    </Grid>
    </ScrollViewer>
</UserControl>
