<UserControl x:Class="Rogue.NET.ScenarioEditor.Views.Assets.Layout"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:toolkit="clr-namespace:Xceed.Wpf.Toolkit;assembly=Xceed.Wpf.Toolkit"   
             xmlns:views="clr-namespace:Rogue.NET.Common.View;assembly=Rogue.NET.Common"
             xmlns:coreModelEnums="clr-namespace:Rogue.NET.Core.Model.Enums;assembly=Rogue.NET.Core"
             mc:Ignorable="d" Background="Black"             
             FontSize="14"  d:DesignWidth="550">
    <UserControl.Resources>
        <ResourceDictionary Source="pack://application:,,,/Rogue.NET.Common;component/Resource/Dictionaries/ButtonSpinner.xaml" />
    </UserControl.Resources>
    <Grid>
        <StackPanel>
            <Grid Margin="10">
                <TextBlock Text="Type" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Left" />
                <views:EnumComboBox EnumValueChanged="EnumComboBox_EnumValueChanged" EnumValue="{Binding Type, Mode=TwoWay}" EnumType="{x:Type coreModelEnums:LayoutType}" Width="150" HorizontalAlignment="Right" />
            </Grid>

            <GroupBox x:Name="HiddenDoorGB" Margin="10" Header="Hidden Door Parameters">
                <StackPanel>
                    <TextBlock Margin="10" Foreground="White" TextWrapping="Wrap"  Text="Hidden doors are found by searching in an adjacent cell. The counter is set at random uniformly between 3-20 search tries." />
                    <Grid Margin="10,5,10,5">
                        <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="White" Text="Hidden Door Probability" />
                        <toolkit:DoubleUpDown Watermark="Enter a Value" 
                                                Minimum="0" 
                                                Maximum="1" 
                                                Increment="0.01" 
                                                FormatString="F2" 
                                                Foreground="White"
                                                HorizontalContentAlignment="Left"
                                                Value="{Binding HiddenDoorProbability, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                                Width="150" HorizontalAlignment="Right"  />
                    </Grid>
                </StackPanel>
            </GroupBox>

            <GroupBox x:Name="MazeGB" Margin="10" Header="Maze Parameters">
                <StackPanel>
                    <Viewbox>
                        <TextBlock Margin="10" Foreground="White" TextWrapping="Wrap"  Text="Mazes are built using a recursive backtracking algorithm which has a chance to leave very few paths to the end. To make the maze easier you can remove walls at random. It is recommended that you try different numbers and see the effect on the maze before uploading your scenario. It is recommended that you try different numbers and see the effect on the maze before uploading your scenario." />
                    </Viewbox>
                    <Grid Margin="10,5,10,5">
                        <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="White" Text="Extra Wall Removals" />
                        <toolkit:IntegerUpDown 
                                                Watermark="Enter a Value" 
                                                Minimum="0" 
                                                Maximum="10000" 
                                                Increment="10" 
                                                FormatString="F2" 
                                                Foreground="White"
                                                HorizontalContentAlignment="Left"
                                                Value="{Binding NumberExtraWallRemovals, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                                Width="150" HorizontalAlignment="Right"  />
                    </Grid>
                </StackPanel>
            </GroupBox>

            <GroupBox x:Name="RoomGB" Margin="10"  Header="Room Grid Parameters">
                <StackPanel>
                    <TextBlock Margin="10" Foreground="White" TextWrapping="Wrap"  Text="Each layout type is built on a grid of rooms. You must specify the size of the grid by setting the number of rows and columns as well as each cell's size." />
                    <Grid Margin="10,5,10,5">
                        <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="White" Text="Room Grid Columns" />
                        <toolkit:IntegerUpDown Watermark="Enter a Value" 
                                                Minimum="2" 
                                                Maximum="20" 
                                                Increment="1" 
                                                FormatString="N0" 
                                                Foreground="White"
                                                HorizontalContentAlignment="Left"
                                                Value="{Binding NumberRoomCols, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                                Width="150" HorizontalAlignment="Right"  />
                    </Grid>
                    <Grid Margin="10,5,10,5">
                        <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="White" Text="Room Grid Rows" />
                        <toolkit:IntegerUpDown Watermark="Enter a Value" 
                                                Minimum="2" 
                                                Maximum="20" 
                                                Increment="1" 
                                                FormatString="N0" 
                                                Foreground="White"
                                                HorizontalContentAlignment="Left"
                                                Value="{Binding NumberRoomRows, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                                Width="150" HorizontalAlignment="Right"  />
                    </Grid>
                    <Grid Margin="10,5,10,5">
                        <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="White" Text="Room Width" />
                        <toolkit:IntegerUpDown Watermark="Enter a Value" 
                                                Minimum="2" 
                                                Maximum="40" 
                                                Increment="1" 
                                                FormatString="N0" 
                                                Foreground="White"
                                                HorizontalContentAlignment="Left"
                                                Value="{Binding RoomDivCellWidth, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                                Width="150" HorizontalAlignment="Right"  />
                    </Grid>
                    <Grid Margin="10,5,10,5">
                        <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="White" Text="Room Height" />
                        <toolkit:IntegerUpDown Watermark="Enter a Value" 
                                                Minimum="2" 
                                                Maximum="40" 
                                                Increment="1" 
                                                FormatString="N0" 
                                                Foreground="White"
                                                HorizontalContentAlignment="Left"
                                                Value="{Binding RoomDivCellHeight, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                                Width="150" HorizontalAlignment="Right"  />
                    </Grid>
                </StackPanel>
            </GroupBox>
        </StackPanel>
    </Grid>
</UserControl>
