<UserControl x:Class="Rogue.NET.ScenarioEditor.Views.ScenarioAssetBrowser"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Rogue.NET.ScenarioEditor;component/Resources/Dictionaries/ScenarioAssetBrowser.xaml" />
                <ResourceDictionary Source="pack://application:,,,/Rogue.NET.ScenarioEditor;component/Resources/Dictionaries/Editor.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid Margin="8">
            <TextBlock Foreground="White" 
                       Text="Scenario Assets" 
                       HorizontalAlignment="Left"
                       FontSize="20" />
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Style="{StaticResource ActionButton}" Command="{Binding CollapseAssetTreeCommand}" ToolTip="Collapse Asset Tree" FontFamily="{StaticResource FontAwesome}" Content="&#xf146;" Margin="2,0,0,0" />
            </StackPanel>                        
        </Grid>
        <TreeView x:Name="AssetTreeView"
                  ItemContainerStyle="{DynamicResource AssetBrowserItemContainerStyle}" 
                  Style="{DynamicResource AssetBrowserStyle}" Grid.Row="1">
            <TreeViewItem x:Name="LayoutItem" ItemsSource="{Binding Assets}" ItemContainerStyle="{DynamicResource AssetBrowserRemoveButtonStyle}" DataContext="{Binding LayoutGroup}" Style="{DynamicResource AssetBrowserAddButtonStyle}" Tag="Layout" Header="Layouts" />
            <TreeViewItem x:Name="EnemiesItem" ItemsSource="{Binding Assets}" ItemContainerStyle="{DynamicResource AssetBrowserRemoveButtonStyle}"  DataContext="{Binding EnemyGroup}" Style="{DynamicResource AssetBrowserAddButtonStyle}" Tag="Enemy" Header="Enemies" />
            <TreeViewItem x:Name="EquipmentItem" ItemsSource="{Binding Assets}" ItemContainerStyle="{DynamicResource AssetBrowserRemoveButtonStyle}" DataContext="{Binding EquipmentGroup}" Style="{DynamicResource AssetBrowserAddButtonStyle}" Tag="Equipment"  Header="Equipment" />
            <TreeViewItem x:Name="ConsumablesItem" ItemsSource="{Binding Assets}" ItemContainerStyle="{DynamicResource AssetBrowserRemoveButtonStyle}" DataContext="{Binding ConsumableGroup}"  Style="{DynamicResource AssetBrowserAddButtonStyle}" Tag="Consumable" Header="Consumables" />
            <TreeViewItem x:Name="DoodadsItem" ItemsSource="{Binding Assets}"  ItemContainerStyle="{DynamicResource AssetBrowserRemoveButtonStyle}" DataContext="{Binding DoodadGroup}" Style="{DynamicResource AssetBrowserAddButtonStyle}" Tag="Doodad" Header="Scenario Objects" />
            <TreeViewItem x:Name="SpellsItem"  ItemsSource="{Binding Assets}" ItemContainerStyle="{DynamicResource AssetBrowserRemoveButtonStyle}" DataContext="{Binding SpellGroup}"  Style="{DynamicResource AssetBrowserAddButtonStyle}"  Tag="Spell" Header="Alterations" />
            <TreeViewItem x:Name="SkillSetsItem"  ItemsSource="{Binding Assets}"  ItemContainerStyle="{DynamicResource AssetBrowserRemoveButtonStyle}" DataContext="{Binding SkillSetGroup}" Style="{DynamicResource AssetBrowserAddButtonStyle}"  Tag="SkillSet" Header="Skill Sets" />
            <TreeViewItem x:Name="AnimationsItem" ItemsSource="{Binding Assets}" ItemContainerStyle="{DynamicResource AssetBrowserRemoveButtonStyle}"  DataContext="{Binding AnimationGroup}" Style="{DynamicResource AssetBrowserAddButtonStyle}"  Tag="Animation" Header="Animations" />
            <TreeViewItem x:Name="BrushesItem" ItemsSource="{Binding Assets}"  ItemContainerStyle="{DynamicResource AssetBrowserRemoveButtonStyle}" DataContext="{Binding BrushGroup}" Style="{DynamicResource AssetBrowserAddButtonStyle}"  Tag="Brush" Header="Brushes" />
        </TreeView>
    </Grid>
</UserControl>
