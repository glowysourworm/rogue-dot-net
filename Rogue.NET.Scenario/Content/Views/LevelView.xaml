<UserControl x:Class="Rogue.NET.Scenario.Views.LevelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Rogue.NET.Scenario.Views"
             xmlns:gadgets="clr-namespace:Rogue.NET.Scenario.Views.Gadgets">
    <UserControl.Resources>
        <ResourceDictionary Source="pack://application:,,,/Rogue.NET.Common;component/Resources/Dictionaries/ShinyButton.xaml" />
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="250" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <local:StatusCtrl Grid.RowSpan="2" VerticalAlignment="Top" />
        <local:PlayerSubpanel x:Name="SubPanel" Grid.Row="2" Height="360" VerticalAlignment="Bottom"/>

        <Border ClipToBounds="True" Grid.Column="1" Grid.RowSpan="3" Margin="8, 0, 0, 0" BorderBrush="Gray" BorderThickness="0.75, 0.75, 0.75, 0.75">
            <local:LevelCanvas x:Name="TheLevelCanvas" />
        </Border>
        
        <gadgets:GadgetContainer x:Name="GadgetContainer" VerticalAlignment="Stretch" VerticalContentAlignment="Top" Grid.Column="2" Grid.RowSpan="3" Margin="8" />
        
    </Grid>
</UserControl>
