<UserControl x:Class="Rogue.NET.Scenario.Content.Views.Gadgets.EquipmentGadget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Background="Black" Margin="0,10,0,0"
             xmlns:scenario="clr-namespace:Rogue.NET.Scenario.Views"
             VerticalAlignment="Stretch" VerticalContentAlignment="Stretch">
    <Grid VerticalAlignment="Bottom">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid>
            <Rectangle Fill="DarkBlue" Opacity="0.3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
            <TextBlock Text="Equipment" Foreground="#FFFFDE87" Opacity="1" Margin="2" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14" />
        </Grid>
        <scenario:ItemGrid x:Name="ItemGrid" VerticalContentAlignment="Stretch" Grid.Row="1" Mode="Equipment" MinHeight="300" IntendedAction="Equip" />
    </Grid>
</UserControl>
